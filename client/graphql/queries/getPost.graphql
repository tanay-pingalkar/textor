query getPost($postId: Float!) {
  getPost(postId: $postId) {
    title
    body
    id
    upvoted
    downvoted
    totalVotes
    user {
      name
    }
    comment {
      id
      body
      user {
        name
      }
      children
      parent
    }
  }
}
