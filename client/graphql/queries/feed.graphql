query feed($lastPostId: String) {
  feed(feedInfo: { lastPostId: $lastPostId }) {
    upvoted
    downvoted
    post {
      title
      body
      totalVotes
      user {
        name
      }
    }
  }
}
