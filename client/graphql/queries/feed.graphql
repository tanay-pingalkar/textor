query feed($lastPostId: String) {
  feed(lastPostId: $lastPostId) {
    upvoted
    downvoted
    post {
      id
      title
      body
      totalVotes
      user {
        name
      }
    }
  }
}
