query profile($username: String!) {
  profile(username: $username) {
    msg
    me
    user {
      name
      reputation
      email
      posts {
        upvoted
        downvoted
        id
        title
        body
        totalVotes
        discussion
      }
    }
  }
}
