query profile($username: String!) {
  profile(username: $username) {
    msg
    user {
      name
      email
    }
    posts {
      upvoted
      downvoted
      post {
        id
        title
        body
        totalVotes
      }
    }
  }
}
