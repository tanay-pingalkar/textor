query profile($username: String!) {
  profile(username: $username) {
    msg
    me
    user {
      name
      email
      posts {
        upvoted
        downvoted
        id
        title
        body
        totalVotes
      }
    }
  }
}
